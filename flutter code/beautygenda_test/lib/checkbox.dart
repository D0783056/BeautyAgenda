import 'package:flutter/material.dart';

class Checkbox extends StatelessWidget {
  final Color colour;
  final String mood;
  final Function onPress;
  Checkbox({@required this.mood,this.colour, this.onPress});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Checkbox(


      ),
    );
  }
}

